//import web3user from "../web3functions/authentication"
//import ipfsModule from "../ipfsCalls/index"
/*
const user = (function(){

  function groupProductsBySeller(products){
    return products.sort((a, b) => a.seller<b.seller)
  }

  function splitInSellerArray(products){
    var productAux = [products[0].id];
    var prevSeller = products[0].seller;
    var result = [[]]
    for(let i = 0; i< products.length; i++){
      if(products[i].seller){

      }
    }
  }
  */

  /*return {
    buy: function(cartInfo){

      //get all the products
      //var products = cartInfo.cart;
      //cartInfo contains all the information about the order

      //sort the products by seller
      //products = groupProductsBySeller(products)

      //carica su ipfs e prendi il CID degli n ordini

      //chiama approve

      //array di remainingHash dei prodotti
      //array di quantità
      //array di hash ordine (x3)
      //periodo

      //chiama payAndSaveOrder
    }
  }
}());

export default user;
*/
